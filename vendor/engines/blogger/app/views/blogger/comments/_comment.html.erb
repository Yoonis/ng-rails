<!-- The partial rendered via @article.comments from article#show page. The comments collection from @article.comments comes with a comment_counter local variable that Rails will auto-instantiate when called. This counter is incremented each time the partial is called, until all comments are rendered. -->
<%= comment_counter +1 %>, <%= comment.text %>
